---

eos_provider:
  username: "{{ un }}"
  password: "{{ pwd }}"
  host: "{{ inventory_hostname }}"

snmp:
  contact: nano_road
  location: NY
  ro:
    - public123
    - london456
  rw:
    - private123
    - nano456
